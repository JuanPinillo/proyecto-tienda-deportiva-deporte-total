name: ProyectoActions

on: 
  push:  # Se activa al hacer un push
    branches: [ Arcila24 ]
    
jobs:
  build:
    runs-on: ubuntu-latest 

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Print a message
      run: echo "Funciona Actions"
